import { ErrorInterceptorInterceptor } from './error-interceptor.interceptor';

describe('ErrorInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ErrorInterceptorInterceptor()).toBeDefined();
  });
});
